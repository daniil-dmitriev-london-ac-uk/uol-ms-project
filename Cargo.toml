[package]
name = "heapstore"
version = "0.1.0"
edition = "2024"
description = "Embedded heap storage layer for variable-size records on NVMe/Linux (UOL MSc project)"

[[bin]]
name = "bench"
path = "src/bin/bench.rs"

[dependencies]
libc = "0.2"
io-uring = "0.7"

[workspace]
